# Assembly Robot Controllers Configuration
# Bu dosya ros2_control controller_manager için yapılandırma içerir

controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    joint_trajectory_controller:
      type: joint_trajectory_controller/JointTrajectoryController

joint_state_broadcaster:
  ros__parameters:
    joints:
      - Revolute 20
      - Revolute 21
      - Revolute 22
      - Revolute 23
      - Revolute 24

joint_trajectory_controller:
  ros__parameters:
    joints:
      - Revolute 20
      - Revolute 21
      - Revolute 22
      - Revolute 23
      - Revolute 24

    command_interfaces:
      - position

    state_interfaces:
      - position
      - velocity

    state_publish_rate: 50.0
    action_monitor_rate: 20.0

    allow_partial_joints_goal: false
    open_loop_control: false
    
    constraints:
      stopped_velocity_tolerance: 0.01
      goal_time: 0.0
